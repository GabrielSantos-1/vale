export function rateLimitPlaceholder() {
  // Implement rate limiting with an in-memory or external store
  return { ok: true }
}

export default rateLimitPlaceholder
// placeholder rate limit
export function rateLimit() {
  // TODO: integrate with a store or middleware
}
